# Copie para `.env` e preencha (não commite segredos).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Opcional: Sentry (DSN público do projecto — configurar também no EAS para builds).
EXPO_PUBLIC_SENTRY_DSN=
